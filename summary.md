# Python Lessons 02-15_16 Summary

這份整理只保留基礎到中階重點，並用「資料分析會怎麼用到這些能力」來重組。

## 1. 從資料分析角度看，這套課在學什麼

- 前半段：學會用 Python 處理單一值、文字、清單、條件判斷、迴圈。
- 中段：學會把邏輯包成函式、讀懂錯誤、處理檔案。
- 後段：開始接觸真正的資料分析流程，包含 CSV、pandas、NumPy、簡單視覺化。
- 核心觀念不是背語法，而是把「原始資料」變成「可分析的結果」。

## 2. 資料分析常見工作流

1. 讀資料：`input()`、檔案、CSV。
2. 看資料：型別、欄位、前幾筆、資料大小。
3. 清資料：去空白、轉型、處理缺值、修正格式。
4. 算指標：總和、平均、最大最小、計數、分組統計。
5. 篩資料：用條件判斷、迴圈、遮罩找出想看的資料。
6. 輸出結果：印出、存檔、畫圖。

## 3. Lesson 02 基礎語法、變數、型別

### 重點

- 變數是資料的名稱，之後分析資料時會一直用來存結果。
- 常見型別：`int`、`float`、`str`、`bool`。
- 型別轉換很重要：`int()`、`float()`、`str()`。
- `print()` 用來檢查結果，`input()` 讀進來的資料預設是字串。
- 常用運算：`+ - * / // % **`、比較運算、邏輯運算。

### 資料分析視角

- 很多資料一開始看起來像數字，其實是字串，必須先轉型才能分析。
- `print()` 是最基本的 debug 工具，初學分析時非常重要。
- 平均、比例、成長率都建立在正確型別之上。

## 4. Lesson 03 字串、串列、條件判斷

### 字串重點

- 常用方法：`upper()`、`lower()`、`replace()`、`split()`、`strip()`。
- 切片：`text[start:end:step]`。
- 成員判斷：`in`。

### 串列重點

- 用來存多筆同類型資料。
- 常見操作：索引、切片、`append()`、`insert()`、`remove()`、複製。
- 了解「直接指定」和「複製」的差別，避免不小心改到原資料。

### 條件判斷重點

- `if`、`if-else`、`if-elif-else`。
- 邏輯運算：`and`、`or`、`not`。

### 資料分析視角

- 字串清理是資料清理的基本功，例如去空白、切開 CSV 文字、統一大小寫。
- 串列常用來暫存一批成績、訂單、名稱、日期。
- 條件判斷是資料篩選的核心，例如找出及格、異常、VIP、缺值。

## 5. Lesson 04 for / while 迴圈

### 重點

- `for` 適合走訪範圍、串列、字串。
- `while` 適合「條件成立就繼續做」的情境。
- `range()` 常用於重複執行。
- `enumerate()` 可同時拿到索引和值。
- `break` 是提前跳出迴圈，`continue` 是跳過這次。

### 常見練習型任務

- 加總、平均、最大值、最小值。
- 計數，例如母音數量、特定字元出現次數。
- 找第一個符合條件的值。

### 資料分析視角

- 迴圈是最早期的資料處理方式，適合幫助理解資料怎麼一筆一筆被處理。
- 雖然 pandas / NumPy 很多時候可取代迴圈，但先懂迴圈才能懂統計計算本質。
- 像計算總營收、掃過訂單找異常值，本質上都是迴圈思維。

## 6. Lesson 05 函式、回傳值、錯誤觀念、遞迴

### 函式 def

- 用 `def` 把重複邏輯包起來。
- 函式可接收參數，也可以 `return` 結果。
- `return` 一執行，整個函式立刻結束。

### 錯誤與除錯

- 常見錯誤：`SyntaxError`、`NameError`、`TypeError`、`ValueError`、`IndexError`、`ZeroDivisionError`。
- 讀錯誤訊息要看三件事：錯誤類型、哪一行、訊息內容。

### 遞迴

- 只要知道概念即可：要有終止條件、問題要越變越小。
- 在基礎資料分析裡不是主力技能，可先理解，不必深挖。

### 資料分析視角

- 函式很適合包成「清理欄位」、「計算指標」、「判斷資料是否合法」。
- 能讀錯誤訊息，才有辦法處理真實資料裡常見的髒資料與格式問題。

## 7. Lesson 06 排序、字典、例外處理

### 排序

- `sorted()` 會回傳新資料。
- `list.sort()` 會直接改原本的串列。
- 可用 `reverse=True` 反向排序。
- 可用 `key=` 自訂排序規則。

### 字典 dict

- `dict` 是 `key: value` 配對，適合存欄位化資料。
- 常見操作：查詢、新增、更新、刪除。
- `get()` 可安全取值，避免 `KeyError`。
- `keys()`、`values()`、`items()` 很常用。

### try / except

- 程式執行時可能遇到壞資料，不能只假設輸入永遠正確。
- 可針對不同例外做不同處理，例如 `ValueError`、`ZeroDivisionError`。

### 資料分析視角

- 排序常用來找前幾高、前幾低、排名。
- 字典很適合做分類統計，例如每個商品的銷量、每個字元的出現次數。
- `try/except` 很適合處理資料轉型失敗、欄位缺漏、索引超界。

## 8. Lesson 07 檔案處理

### 重點

- 用 `open()` 開檔，常見模式：`r`、`w`、`a`。
- 讀取方法：`read()`、`readline()`、`readlines()`。
- 寫入方法：`write()`、`writelines()`、`print(..., file=f)`。
- 讀寫完要 `close()`，更好的習慣是用 `with open(...) as f:`。

### 資料分析視角

- 真實資料常常先以文字檔、CSV 檔存在磁碟。
- 檔案處理是從「程式練習」走向「真實資料工作」的關鍵一步。
- 例如讀取名單、寫分析結果、輸出整理後資料。

## 9. Lesson 08 模組 module

### 重點

- 模組就是可重複使用的 Python 工具箱。
- 常見匯入方式：`import math`、`import statistics as st`。
- 會用到內建模組的函式與變數，例如 `math.sqrt()`、`math.pi`、`statistics.mean()`。

### 資料分析視角

- 分析很少只靠原生語法，一定會用模組。
- 這一課是在為後面的 `pandas`、`matplotlib`、`numpy` 鋪路。

## 10. Lesson 09 OOP 基礎觀念

### 建議保留的基礎重點

- 知道 `class` 是藍圖，`object` 是實例。
- 知道 `__init__` 用來建立物件初始資料。
- 知道屬性是資料，方法是行為。

### 資料分析視角

- 一般初階資料分析不一定常自己寫 class。
- 但理解物件概念有助於看懂 pandas `DataFrame`、NumPy `ndarray` 其實都是物件。
- 這課先抓概念即可，進階繼承、覆寫可先略過。

## 11. Lesson 11-12 資料分析入門：pandas + matplotlib

### 重點

- `import pandas as pd`
- `import matplotlib.pyplot as plt`
- 用 `pd.read_csv()` 讀資料。
- 先用 `head()` 看前幾筆資料。
- 認識欄位名稱、資料列數、資料結構。
- 日期欄位要轉成可分析格式。
- 開始做簡單統計與畫圖。

### 資料分析視角

- 這是第一個完整分析流程：讀資料 -> 看資料 -> 整理 -> 分析 -> 畫圖。
- 先確認資料長什麼樣，比急著算答案更重要。
- 圖表的功能是幫助看趨勢，不只是好看。

## 12. Lesson 13-14 pandas 資料清理與 profiling

### 重點

- `df.head()`、`df.tail()`、`df.sample()` 快速抽查資料。
- `df.shape` 看資料大小。
- `df.columns` 看欄位名稱。
- `df.info()` 看型別與缺值狀況。
- `df.describe(include='all')` 看統計摘要。
- 注意資料清理問題：
  - 數字欄位被讀成 `object`
  - 日期格式不一致
  - 大小寫不一致
  - 缺值
  - 類別值不一致

### 資料分析視角

- 真實分析常常不是先做模型，而是先做 profiling。
- 看到 `object` 欄位時，要先懷疑是不是髒資料或格式不一致。
- 缺值不一定都刪，要看欄位的重要性決定補值、推算或刪除。
- 清理資料的品質，會直接決定分析結果是否可信。

## 13. Lesson 15-16 NumPy 資料分析

### 重點

- NumPy 核心結構是 `ndarray`。
- 常見屬性：`ndim`、`shape`、`dtype`。
- 建立資料：`zeros`、`ones`、`full`、`arange`、`linspace`、隨機資料。
- 索引與切片和 list 類似，但更快。
- 布林遮罩是分析重點，可篩出符合條件的資料。
- 多條件要用 `&`、`|`、`~`，而不是 `and`、`or`、`not`。
- 向量化運算比手寫迴圈更適合大量數值分析。
- 常見統計：`mean`、`sum`、`min`、`max`、`std`、排序、分箱。

### 資料分析視角

- pandas 很方便，但底層很多運算都靠 NumPy。
- NumPy 適合純數值、批量、快速的資料處理。
- 學 NumPy 的真正價值是建立「整批資料一起算」的思維。

## 14. 最值得帶去做投影片的核心能力

### 基礎能力

- 變數、型別、型別轉換。
- 字串清理。
- 串列與字典操作。
- 條件判斷。
- `for` / `while` 迴圈。
- 函式與 `return`。
- 看懂錯誤訊息。

### 中階能力

- 排序與分類統計。
- 檔案讀寫。
- 模組匯入。
- 用 pandas 讀 CSV、檢查資料、清理資料。
- 用 NumPy 做快速數值運算與篩選。
- 基本視覺化。

## 15. 如果用一句話總結這套課

這套課的主線是：先學會 Python 基本語法與資料結構，再把這些能力接到真實的表格資料上，最後用 pandas 和 NumPy 做出最基本但完整的資料分析流程。

## 16. 給 ChatGPT 產投影片時可強調的方向

- 觀眾是 Python 初學者。
- 不要放太難的演算法。
- 重點放在「資料分析為什麼需要這些語法」。
- 每個主題最好搭配 1 個生活化或商業資料案例。
- 投影片順序可依照：基礎語法 -> 資料結構 -> 控制流程 -> 函式與除錯 -> 檔案 -> pandas -> NumPy -> 小型分析流程。
